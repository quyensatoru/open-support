# MIDA Agent MCP Placeholder

This folder is intentionally empty of real MCP tools in v1.

The future MCP server should expose selected `mida-agent` capabilities to external systems:

- create and inspect agent runs
- list public tools and skills
- stream run status or tool results

It must not connect to `mida-mcp` in this scaffold phase.
